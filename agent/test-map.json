{
  "version": 1,
  "workspace": "jmcp-deploy",
  "tests": {
    ".autonomy/": {
      "command": "just score",
      "purpose": "verify autonomy policy remains routed"
    },
    ".editorconfig": {
      "command": "just fast",
      "purpose": "verify repository editor policy stays parseable"
    },
    ".env.example": {
      "command": "just fast",
      "purpose": "verify sample environment config remains routed"
    },
    ".github/workflows/": {
      "command": "just security",
      "purpose": "verify workflow automation stays routed through the security lane"
    },
    ".gitignore": {
      "command": "just fast",
      "purpose": "verify repository ignore policy"
    },
    ".jeryu/": {
      "command": "just fast",
      "purpose": "verify local Jeryu metadata remains routed"
    },
    ".npmrc": {
      "command": "just fast",
      "purpose": "verify npm configuration remains routed"
    },
    "AGENTS.md": {
      "command": "just score",
      "purpose": "verify root agent routing remains visible"
    },
    "CHANGELOG.md": {
      "command": "just release-readiness",
      "purpose": "verify changelog remains part of release evidence"
    },
    "CLAUDE.md": {
      "command": "just score",
      "purpose": "verify Claude-facing instructions remain routed"
    },
    "GEMINI.md": {
      "command": "just score",
      "purpose": "verify Gemini-facing instructions remain routed"
    },
    "Justfile": {
      "command": "just fast",
      "purpose": "verify local proof targets remain routed"
    },
    "README-jankurai-scaffold.md": {
      "command": "just score",
      "purpose": "verify Jankurai scaffold remains routed"
    },
    "README.md": {
      "command": "just score",
      "purpose": "verify README guidance remains routed"
    },
    "agent/": {
      "command": "just score",
      "purpose": "verify agent-readable manifests remain parseable"
    },
    "ci-fast-push.sh": {
      "command": "just fast",
      "purpose": "verify local fast-push helper remains shell-parseable"
    },
    "docs/": {
      "command": "just release-readiness",
      "purpose": "verify documentation remains aligned with release proof"
    },
    "gitleaks.toml": {
      "command": "just security",
      "purpose": "verify secret-scanning policy remains routed"
    },
    "jeryu-ctl/": {
      "command": "just fast",
      "purpose": "verify Jeryu control scripts remain shell-parseable"
    },
    "ops/": {
      "command": "just fast",
      "purpose": "verify local CI and split orchestration scripts"
    },
    "ops/split/": {
      "command": "just health-offline && just launch-dry-run",
      "purpose": "verify offline split launch and health checks"
    },
    "repos.manifest.toml": {
      "command": "just contract-drift",
      "purpose": "verify split repository manifest wiring"
    },
    "scripts/": {
      "command": "just fast",
      "purpose": "verify environment scripts remain shell-parseable"
    },
    "tests/": {
      "command": "just test",
      "purpose": "verify manifest and launch unit tests"
    },
    "tools/": {
      "command": "just fast",
      "purpose": "verify local tool hooks remain shell-parseable"
    }
  }
}
